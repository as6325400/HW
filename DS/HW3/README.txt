首先先建立兩個 link list，然後讀輸入的時候從後面讀回來，把位數比較低的那個在高位的地方補 0

相加兩個數列，因為位數一樣，所以可以直接加，也因為剛剛讀輸入的時候是從背後往前讀的，因此相加後要用 push_front() ，這樣得到的最終結果 link list方向才是對的

接下來是難做的 reverse 部分

假設 
R=2

1 -> 2 -> 3 -> 4 -> 5 -> 6 -> 7 -> 8 -> NULL

那我會先兩個兩個一組

變成

NULL <- 1 <- 2  
NULL <- 3 <- 4  
NULL <- 5 <- 6 
NULL <- 7 <- 8 

變成這種形式
之後再分別把他串起來


串起來的函示在 reverse()
分段反轉的函示為 r()